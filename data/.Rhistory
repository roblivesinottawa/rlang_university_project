test <- c(25, 35, 40, 50, 75)
print(test)
mean(test)
employee <- list(name = "juan", age = 30)
employee
employee["age"] == 35
employee <- list(name = "juan", age = 30, title = "manager")
employee
employee$title
word_string <- "the quick fox jumped over the lazy dog"
word_string
nchar("dog") <- "cat"
strsplit("dog") <- "cat"
substr("dog") <- "cat"
chartr("dog") <- "cat"
substr("cat", "dog", word_string)
word_string
chartr("cat", "dog", word_string)
nchar("cat", "dog", word_string)
strsplit("cat", "dog", word_string)
word_string
chartr("dog", "cat", word_string)
tryCatch(
name <- "Robson"
if name == "Robson":
tryCatch(
user <- "Rob"
if (user == "Rob") {print("Welcome back Rob")} else {print("an error ocurred")})
q()
